// Stubbed client: replace with real API calls

export async function fetchStats(
    sportSlug: string,
    statKey: string
  ): Promise<number> {
    // TODO: Call a free sports API based on sportSlug & statKey.
    // For now, return a random number to simulate.
    return Math.random() * 50;
  }
  